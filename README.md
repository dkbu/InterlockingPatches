# InterlockingPatchMaker

## Debugging
- To enable debugging mode, remove `example` from the `localdev.js.example`
- To run testing locally use `python -m http.server 8000` and access on `http://127.0.0.1:8000/`

